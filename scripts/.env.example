# --------------------------------------------------------------------
# Frontend configuration
# --------------------------------------------------------------------

# Base URL for your backend API
# In Docker or prod, update to the container or deployed API URL.
VITE_API_URL=http://localhost:3000

# Authentication mode:
#   session → use httpOnly secure cookies
#   jwt     → use JSON Web Tokens (Cybersecurity module requires this)
VITE_AUTH_MODE=session


# --------------------------------------------------------------------
# Backend configuration (if you implement a backend)
# --------------------------------------------------------------------

# SQLite database file (if you use the Database module)
DB_FILE=./data/app.sqlite

# Secret key for signing JWT tokens (DO NOT commit a real one in .env!)
# Each teammate should generate their own for local dev.
JWT_SECRET=changeme_in_local_env

# 2FA issuer name (what appears in authenticator apps)
TWOFA_ISSUER=Transcendence


# --------------------------------------------------------------------
# OAuth (for Remote Authentication module, optional)
# --------------------------------------------------------------------

# Example values for GitHub OAuth — leave blank until you set up
OAUTH_CLIENT_ID=
OAUTH_CLIENT_SECRET=
OAUTH_REDIRECT_URI=http://localhost:3000/auth/callback


# --------------------------------------------------------------------
# Misc
# --------------------------------------------------------------------

# Node environment mode: development, production, test
NODE_ENV=development
